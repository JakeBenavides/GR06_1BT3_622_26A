<?xml version='1.0' encoding='utf-8'?>

<hibernate-configuration>
    <session-factory>

        <!-- Conexión MySQL -->
        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>

        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/poliservis?useSSL=false&amp;serverTimezone=UTC&amp;characterEncoding=utf8&amp;allowPublicKeyRetrieval=true
        </property>

        <property name="hibernate.connection.username">root</property>

        <property name="hibernate.connection.password">12345</property>

        <!-- Dialecto MySQL 8 -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQLDialect
        </property>

        <!-- NO recrear tablas, solo actualizar si faltan columnas -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mostrar SQL en consola (útil para depurar) -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Entidades mapeadas -->
        <mapping class="modelo.Usuario"/>
        <mapping class="modelo.Servicio"/>
        <mapping class="modelo.Categoria"/>

    </session-factory>
</hibernate-configuration>